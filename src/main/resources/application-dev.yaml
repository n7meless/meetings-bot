spring:
  datasource:
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/meetings
  data:
    redis:
      host: localhost
      port: 6379